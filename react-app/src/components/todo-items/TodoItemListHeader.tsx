function TodoItemListHeader() {
    return (
        <div className={"todo-item-list-header"}>
            Header - not needed? replace with filter?
        </div>
    )
}

export default TodoItemListHeader